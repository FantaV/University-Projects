seq(from = 1, to = 10, by = 2)
seq( 1, 10, 2)

x <- 7
print(x)

x # AUTO-PRINTING: non è necessario scrivere print 

test <- "Hello World"
test

y <- 1:20
y

k <- exp(2)
k

log10(1000)

log2(10)

sin(45)
cos(0)
tan(33)
acos(12)

# VETTORE: assegna ad x tutti i valori di c
x <- c(4, 9, 7, 1)
x

# accesso all'elemento del vettore
x[2]

# OPERAZIONI CON DUE VETTORI
y <- c(4, 5, 9, 1)  # devono avere stessa struttura (lunghezza)
x - y

length(x)

max(x) 
min(x) 
mean(x)
which.min(x) # indica la posizione del minimo all'interno del vettore 


9 == 7

x <- 5
y <- 3
x == y
